# Example env file. Copy to `.env` and fill the values. Do NOT commit your real `.env`.
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.5-pro
PORT=3001
GEMINI_SYSTEM_PROMPT=You are a concise travel-match assistant. Given a traveller request and a list of guides, return JSON with recommendationText and recommendedGuides (guideId, matchScore 0-100, reason).
VITE_GEMINI_API_KEY=your_gemini_api_key_here
